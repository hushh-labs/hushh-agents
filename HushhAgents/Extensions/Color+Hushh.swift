import SwiftUI

extension Color {
    /// Primary brand color — Apple blue
    static let hushhPrimary = Color.blue

    /// Pass / Reject action color — Apple red
    static let hushhPass = Color.red

    /// Like / Select action color — Apple green
    static let hushhLike = Color.green

    /// Claim / Star action color — Apple orange
    static let hushhClaim = Color.orange

    /// Card background — pure white
    static let hushhCard = Color.white

    /// Page background — light gray
    static let hushhBackground = Color(UIColor.systemGroupedBackground)
}
